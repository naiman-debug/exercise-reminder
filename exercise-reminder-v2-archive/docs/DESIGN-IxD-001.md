# DESIGN-IxD-001 交互设计文档

**项目**："灵动休息"健康助手
**版本**：v2.0
**日期**：2026-01-27
**设计者**：Claude Code

---

## 一、 设计原则

### 1.1 核心原则
- **强制但友好**：强制休息，但不让用户产生反感
- **简单直观**：所有交互在 3 秒内可理解
- **最小干扰**：提醒清晰明确，完成后立即消失
- **正反馈**：完成动作后给予即时反馈

### 1.2 设计目标
- 解决久坐问题，培养健康习惯
- 最小化学习成本，开箱即用
- 提供足够定制性，满足不同需求

---

## 二、 用户旅程

### 2.1 首次使用流程

```
启动应用
    ↓
个人基础设置（身高、体重、年龄、性别、每日目标）
    ↓
运动间隔时间设置（提醒间隔 + 执行时长）
    ↓
体验提示页（10秒倒计时）
    ↓
倒计时结束 → 弹出首次站立提醒
```

### 2.2 日常使用流程

```
系统托盘常驻
    ↓
定时触发提醒（随机间隔: interval_avg ± offset）
    ↓
全屏倒计时弹窗（自动倒计时结束）
    ↓
弹窗关闭，记录数据
    ↓
进入2分钟冷却期
    ↓
继续工作，等待下次提醒
```

---

## 三、 核心交互设计

### 3.1 系统托盘交互

| 图标状态 | 含义 | 点击行为 |
|---------|------|---------|
| 🟢 绿色 | 正常运行 | 显示菜单 |
| 🟡 黄色 | 冷却中 | 显示菜单 |
| 🔵 蓝色 | 暂停状态 | 显示菜单 |

**托盘菜单**：
```
┌─────────────────────┐
│  灵动休息           │
├─────────────────────┤
│  🏠 打开主窗口      │
│  📊 查看统计        │
│  ⚙️  设置           │
│  📋 动作库管理      │
├─────────────────────┤
│  ⏸️  暂停提醒       │
│  ▶️  恢复提醒       │
├─────────────────────┤
│  ❌ 退出            │
└─────────────────────┘
```

### 3.2 强制站立弹窗

**设计规范**：
- 尺寸：全屏 60%
- 位置：屏幕正中央
- 字体：超大号（72pt+）
- 置顶：强制置顶（StayOnTop）
- 关闭：无关闭按钮，无跳过按钮

**交互流程**：
```
弹窗出现（淡入动画 0.3s）
    ↓
倒计时开始（大字体显示）
    ↓
可选：播放提示音
    ↓
倒计时进行中...
    ↓
倒计时归零
    ↓
显示"完成"反馈（0.5s）
    ↓
弹窗消失（淡出动画 0.3s）
```

**倒计时格式**：
```
   MM:SS  或  SS 秒
   02:30      90 秒
```

### 3.3 微运动弹窗

**设计规范**：
- 尺寸：800 x 600 px
- 位置：屏幕中央
- 字体：动作名称 48pt，倒计时 60pt
- **无按钮**：自动倒计时结束

**交互流程**：
```
弹窗出现（淡入动画 0.3s）
    ↓
显示当前动作 + 倒计时
    ↓
倒计时进行中...
    ↓
倒计时归零
    ↓
显示完成反馈 + 热量消耗（1s）
    ↓
弹窗消失（淡出动画 0.3s）
```

**重要变化**：
- ❌ 去掉"完成"、"跳过"、"换一个"按钮
- ✅ 纯倒计时自动结束
- ✅ 显示热量消耗反馈

### 3.4 强制远眺弹窗

**设计规范**：
- 尺寸：全屏 50%
- 位置：屏幕中央
- 背景：可选显示放松图片/渐变
- 倒计时：固定 60 秒

**特殊交互**：
- 可配置背景音乐（用户可选）
- 音乐与倒计时同步结束
- 倒计时最后 10 秒可配置语音播报

---

## 四、 首次运行向导设计

### 4.1 向导流程

```
第1页: 个人基础设置
    ↓
第2页: 运动间隔时间设置
    ↓
第3页: 体验提示页（10秒倒计时）
    ↓
自动弹出首次站立提醒
```

### 4.2 第1页：个人基础设置

**输入项**：
```
┌────────┐  ┌────────┐  ┌────────┐  ┌────────┐
│ 身高   │  │ 体重   │  │ 年龄   │  │ 性别   │
│ [170]  │  │ [70.0] │  │ [30]   │  │ ⚪男   │
│ 厘米   │  │ 千克   │  │ 岁     │  │ ⚪女   │
└────────┘  └────────┘  └────────┘  └────────┘

┌────────────────────────────────────────────────┐
│  🔥 每日运动目标                                │
│           [ 300 ]  kcal                        │
└────────────────────────────────────────────────┘
```

### 4.3 第2页：运动间隔时间设置

**配置结构**：
```
全局设置:
├─ 统一随机偏移: [ 15 ] 分钟
└─ 说明: 所有间隔 = 平均值 ± 偏移量

提醒间隔:
├─ 强制站立间隔: [ 45 ] 分钟
├─ 微运动间隔: [ 60 ] 分钟
└─ 强制远眺间隔: [ 75 ] 分钟

执行时长:
├─ 强制站立时长: [ 90 ] 秒
├─ 微运动时长: [ 120 ] 秒
└─ 强制远眺时长: [ 60 ] 秒
```

### 4.4 第3页：体验提示页

**布局**：
```
┌────────────────────────────────────────────────┐
│  ✅ 设置完成！                                  │
│                                                │
│  应用将在 [ 10 ] 秒后开始运行                   │
│                                                │
│  ┌──────────────────────────────────────────┐ │
│  │  💡 第一次体验提示：                      │ │
│  │  • 所有提醒都是倒计时自动结束              │ │
│  │  • 站起提醒: 请站立等待倒计时              │ │
│  │  • 微运动: 跟着动作做，等待倒计时          │ │
│  │  • 远眺: 放松眼睛，眺望远方                │ │
│  └──────────────────────────────────────────┘ │
│                                                │
│  [ 跳过体验 ]  [ 立即体验 ]                    │
└────────────────────────────────────────────────┘
```

---

## 五、 设置界面交互

### 5.1 设置主界面结构

```
┌─────────────────────────────────────────────┐
│  设置                              [_][□][X] │
├─────────────────────────────────────────────┤
│  [ 提醒设置 ] [ 用户信息 ] [ 动作库 ]       │
│  [ 统计 ]    [ 基础设置 ]                  │
├─────────────────────────────────────────────┤
│                                             │
│              (当前选中页面的内容)            │
│                                             │
├─────────────────────────────────────────────┤
│              [ 应用 ]  [ 取消 ]             │
└─────────────────────────────────────────────┘
```

### 5.2 提醒设置页（新配置结构）

**输入控件设计**：
```
全局设置
├─ 统一随机偏移: [ 15 ] 分钟
└─ 说明: 所有提醒的实际间隔会在设定值基础上随机增减

提醒间隔设置
├─ 强制站立间隔: [ 45 ] 分钟
├─ 微运动间隔: [ 60 ] 分钟
└─ 强制远眺间隔: [ 75 ] 分钟

执行时长设置
├─ 强制站立时长: [ 90 ] 秒
├─ 微运动时长: [ 120 ] 秒
└─ 强制远眺时长: [ 60 ] 秒

启用提醒
├─ ☑ 启用强制站立提醒
├─ ☑ 启用微运动提醒
└─ ☑ 启用强制远眺提醒
```

### 5.3 基础设置页（新增）

**输入控件设计**：
```
启动设置
├─ ☐ 开机自动运行
├─ ☑ 启动时显示通知
└─ ☑ 关闭窗口时最小化到托盘（不退出）

音频设置
├─ ☑ 启用音效
├─ 音量: [●●●●●○○○○○] 70%
├─ 音效类型: [电子哔声 ▼]
└─ ☐ 倒计时最后5秒语音播报

提醒样式
├─ 窗口位置: [居中 ▼]
└─ 窗口透明度: [●●●●●●●●●●] 100%
```

---

## 六、 反馈设计

### 6.1 即时反馈

| 动作 | 反馈 | 时长 |
|------|------|------|
| 完成站立 | ✓ 绿色对勾动画 | 0.5s |
| 完成运动 | ✓ 绿色对勾 + 热量显示 | 1s |
| 完成远眺 | ✓ 绿色对勾动画 | 0.5s |

### 6.2 声音反馈

| 时机 | 声音类型 | 可配置 |
|------|---------|--------|
| 弹窗出现 | 提示音 | ✓ |
| 倒计时结束 | 完成音 | ✓ |
| 最后5秒 | 语音倒计时 | ✓ |
| 完成动作 | 成功音 | ✓ |

### 6.3 视觉反馈

**倒计时颜色变化**：
```
> 50% 时间：绿色
30-50% 时间：黄色
< 30% 时间：红色
最后 10 秒：闪烁动画
```

---

## 七、 边界情况处理

### 7.1 全屏应用冲突

**问题**：用户正在使用全屏应用（游戏、视频）

**解决方案**：
- 检测到全屏应用时，延迟提醒
- 托盘图标闪烁提示
- 用户退出全屏后立即提醒

### 7.2 系统休眠/唤醒

**处理**：
- 系统休眠时暂停所有定时器
- 唤醒后重置下次提醒时间
- 记录休眠时段，不影响统计

### 7.3 提醒冷却机制

**逻辑**：
```
提醒触发 → 进入2分钟冷却期
冷却期间：暂停其他定时器
冷却结束：恢复定时器
```

### 7.4 应用最小化

**行为**：
- 提醒触发时自动恢复窗口
- 系统托盘图标闪烁
- 播放提示音（如果启用）

---

## 八、 可访问性设计

### 8.1 键盘导航

| 快捷键 | 功能 |
|--------|------|
| Ctrl+S | 打开设置 |
| Ctrl+Q | 退出应用 |
| Ctrl+H | 打开主窗口 |

### 8.2 高对比度模式

支持 Windows 高对比度模式，自动适配颜色。

### 8.3 字体缩放

支持 Windows DPI 缩放（125%, 150%, 200%）。

---

## 九、 动画设计

### 9.1 弹窗动画

```css
淡入：opacity 0 → 1，duration 0.3s
淡出：opacity 1 → 0，duration 0.3s
缩放：scale 0.9 → 1.0，duration 0.2s（弹窗出现时）
```

### 9.2 倒计时动画

**最后 10 秒闪烁**：
```css
@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}
animation: pulse 1s infinite;
```

---

## 十、 交互状态机

### 10.1 应用状态

```
初始设置 → 运行中 → 暂停 → 退出
   ↑___________↓
```

### 10.2 提醒状态

```
空闲 → 触发提醒 → 显示弹窗 → 倒计时中 → 冷却期 → 空闲
```

---

## 十一、 配置结构变更

### 11.1 旧结构（v1.0）

```json
{
  "reminder": {
    "stand": {
      "interval_min": 30,
      "interval_max": 60
    }
  }
}
```

### 11.2 新结构（v2.0）

```json
{
  "reminder": {
    "global_offset_minutes": 15,
    "stand": {
      "enabled": true,
      "interval_avg": 45,
      "duration": 90
    },
    "exercise": {
      "enabled": true,
      "interval_avg": 60,
      "duration": 120
    },
    "gaze": {
      "enabled": true,
      "interval_avg": 75,
      "duration": 60
    }
  }
}
```

### 11.3 计算方式

```
实际间隔 = interval_avg ± global_offset_minutes
例如: 45 ± 15 = 30-60分钟之间随机
```

---

**文档版本**：v2.0
**最后更新**：2026-01-27
**主要变更**：
- 简化首次运行向导为3页
- 去掉惩罚机制
- 所有提醒改为自动倒计时结束
- 配置结构变更：支持统一偏移量
- 新增提醒冷却机制
